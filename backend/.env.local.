# ─────────────────────────────────────────────────────────────────────────────
# backend/.env.local  —  Local development environment
# Rename to .env before running (godotenv.Load() reads .env by default)
# NEVER commit this file — it is gitignored
# Source of truth for production values: ~/Desktop/JC Projects/Env_Control/
# ─────────────────────────────────────────────────────────────────────────────

# ── Server ────────────────────────────────────────────────────────────────────
PORT=8080
ENVIRONMENT=development

# ── Database (Session Pooler port 5432 — per jconline_architecture.md) ────────
DATABASE_URL=postgresql://postgres.nqnhyxdwqwufdgprodcs:Mogxap-fabqi8-dashep@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres

# ── Supabase ──────────────────────────────────────────────────────────────────
SUPABASE_URL=https://nqnhyxdwqwufdgprodcs.supabase.co
# Service role key — Supabase dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5xbmh5eGR3cXd1ZmRncHJvZGNzIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3MzA0MzQxOSwiZXhwIjoyMDg4NjE5NDE5fQ.1MjFW2126UFoeJzceshcrR1IJefglcmlCOhxzBtIxMU

# ── Auth ──────────────────────────────────────────────────────────────────────
# Supabase dashboard → Settings → API → JWT Settings → JWT Secret
JWT_SECRET=FILL_IN_FROM_SUPABASE_DASHBOARD

# ── Meta / WhatsApp Cloud API ─────────────────────────────────────────────────
# Meta for Developers → Your App → Settings → Basic → App Secret
META_APP_SECRET=FILL_IN_FROM_META_DASHBOARD
# Any string — must match what you entered in Meta webhook configuration
META_VERIFY_TOKEN=FILL_IN_YOUR_VERIFY_TOKEN

# ── AI ────────────────────────────────────────────────────────────────────────
# Anthropic console → API keys
ANTHROPIC_API_KEY=FILL_IN_FROM_ANTHROPIC_CONSOLE
AI_MODEL=claude-haiku-4-5-20251001
# Optional: GLM-4-Flash for Chinese-language tenants (free tier available)
ZHIPU_API_KEY=

# ── WebsiteBot (Telegram) ─────────────────────────────────────────────────────
TELEGRAM_WEBBOT_TOKEN=8689900141:AAGCA8uVqR043a3esDqBGeUp35yKdTspk6I
TELEGRAM_WEBBOT_SECRET=chatrecept_webbot_2026
# Optional: Together AI for logo generation (falls back to free Pollinations.ai)
TOGETHER_API_KEY=
# Optional: Cloudflare Pages deployment (currently unused — sites served from Railway)
CF_ACCOUNT_ID=
CF_API_TOKEN=
# Base URL the bot sends to users as the site link
PUBLIC_BASE_URL=https://backend-production-0aa15.up.railway.app
# Free credits granted to new Telegram bot users (default: 1)
WEBBOT_FREE_CREDITS=1

# ── AdminBot (Telegram) ───────────────────────────────────────────────────────
ChatReceptAdminBot_TOKEN=8794630657:AAHzbJOd3xzSHQE8lwDZiLHMotkQHELquac
ChatReceptAdminBot_SECRET=chatreceptrulez
ADMIN_TELEGRAM_USERNAME=JC141319

# ── Stripe (optional — Phase 2 payments) ─────────────────────────────────────
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SUCCESS_URL=http://localhost:3000/dashboard/wallet?success=1
STRIPE_CANCEL_URL=http://localhost:3000/dashboard/wallet

# ── Wallet ────────────────────────────────────────────────────────────────────
# Credits deducted per new 24-hour conversation window
CONVERSATION_CREDIT_COST=1
